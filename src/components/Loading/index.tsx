import React from 'react';

import { LoadingContainer, LoadingAnimation } from './styles';

const Loading = () => {
  return (
    <LoadingContainer>
      <LoadingAnimation />
    </LoadingContainer>
  );
};

export default Loading;
